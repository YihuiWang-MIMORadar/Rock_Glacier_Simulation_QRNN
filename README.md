# Rock Glacier Velocity Prediction with Quantile Regression

This repository contains code for probabilistic velocity estimation of rock glaciers using multi‑source features and quantile regression (XGBoost and QRNN).

## Requirements

Install dependencies:

```bash
pip install -r requirements.txt